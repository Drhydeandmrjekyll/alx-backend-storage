// 4-match
// my comment

// Connect to the specified database
db = db.getSiblingDB("my_db")

// Find and print documents with name="Holberton school" in the school collection
cursor = db.school.find({ "name": "Holberton school" }).forEach(printjson);
